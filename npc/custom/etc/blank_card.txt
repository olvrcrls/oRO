-	script	BlankCard	-1,{
	OnNPCKillEvent:
		// if (rand(99) > 9 || strnpcinfo(3) != "prt_fild01") end; // 10% chance of success, only works on prt_fild01
	
		// .setarray .@Items,6187;  		// Item array
		// set .@rand,rand(1,getarraysize(.@items);
		set .@rng,rand(1,99);

		if (.@rng > 5 && .@rng <= 10) {
			getitem 6187,1; 
			end;
		}
		else if (.@rng <= 5 && .@rng > 1) {
			getitem 6187,2;
			end;
		}
		else if (.@rng == 1) {
			getitem 6187,5;
			end;
		}
	}
	