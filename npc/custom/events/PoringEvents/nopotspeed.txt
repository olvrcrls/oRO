-	script	nospeedrc	-1,{
OnPCLoadMapEvent:
		if(countitem(12016) > 0 || countitem(662) > 0) 
		if (ismounting()) setmounting;
		warp "SavePoint",0,0;
		dispbottom "You got kicked due to an item/s that is not allowed in the event.";
		dispbottom "Restrictions for Speed Potion / Authorization Badge enabled.";
		end;
}

//------Enable map OnPCLoadMapEvent-------------------
poring_rc	mapflag	loadevent