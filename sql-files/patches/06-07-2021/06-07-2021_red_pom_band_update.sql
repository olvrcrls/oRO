UPDATE `item_db_re` SET `script` = 'bonus bMdef,5; bonus bMatkRate,3; bonus2 bAddRace,RC_DemiHuman,5; bonus2 bAddRace,RC_Player,5; bonus2 bSubRace,RC_DemiHuman,5; bonus2 bSubRace,RC_Player,5; if(getrefine() > 4 && getrefine() < 12) { bonus2 bAddRace,RC_DemiHuman,(getrefine() - 4); bonus2 bSubRace,RC_DemiHuman,(getrefine() - 4); bonus2 bAddRace,RC_Player,(getrefine() - 4); bonus2 bSubRace,RC_Player,(getrefine() - 4); } if(getrefine() >= 12) { bonus2 bAddRace,RC_DemiHuman,8; bonus2 bAddRace,RC_Player,8; bonus2 bSubRace,RC_DemiHuman,8; bonus2 bSubRace,RC_Player,8; }' WHERE (`id` = '18601');
